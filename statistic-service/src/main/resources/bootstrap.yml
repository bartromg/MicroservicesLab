spring:
  application:
    name: statistic-service
  cloud:
    config:
      uri: http://config:8888
      fail-fast: true

  rabbitmq:
        host: rabbitmq
        port: 5672
        username: username
        password: password

server:
  port: 9001

#cassandra:
#  contactpoints: statistic-cassandra
#  port: 9042
#  keyspace: event