spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Distributed-labs-team/cloud-config
          search-paths: microservice-data
  application:
    name: config
server:
  port: 8888
